// Crie uma variável chamada `nome` e atribua seu nome a ela. Depois, exiba no console a mensagem: `"Olá, [nome]!"`.
let nome = "Danilo"
console.log("Olá, " + nome + "!")

// Declare duas variáveis `a` e `b` com valores numéricos e exiba no console a soma, subtração, multiplicação e divisão delas.
let a = 10
let b = 5
console.log(a + b)
console.log(a - b)
console.log(a * b)
console.log(a / b)

// Crie um programa que converta uma temperatura de Celsius para Fahrenheit.
let celsius = -10
console.log(celsius)
let fahrenheit = (celsius * 9 / 5) + 32
console.log(fahrenheit)